import { ListSpecializationComponent } from './list-specialization.component'

describe('ListSpecializationComponent', () => {
  it('should mount', () => {
    cy.mount(ListSpecializationComponent)
  })
})